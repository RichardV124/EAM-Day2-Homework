import { TablePostDirective } from './table-post.directive';

describe('TablePostDirective', () => {
  it('should create an instance', () => {
    const directive = new TablePostDirective();
    expect(directive).toBeTruthy();
  });
});
